root: ???
input_path: tests/test_data

datasets:
  test:
    metafile_path: tests/test_data/metadata/scenes.cec1.test.json
    scene_folder: tests/test_data/scenes

num_channels: 3

# disable hydra loggings
defaults:
  - override hydra/job_logging: disabled

hydra:
  output_subdir: Null
  run:
    dir: .
